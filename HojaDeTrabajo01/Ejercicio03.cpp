#include "Ejercicio03.h"
#include<math.h>
#include<cstdlib>
#include<cmath>

using namespace std;

bool Ejercicio03::isPowerOfTwo(int n)
{   
    int nabs = abs(n);
    log
    
    if(log_2nabs == floor(log_2nabs))
}
